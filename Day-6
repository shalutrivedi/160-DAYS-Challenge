class Solution {
  public:
    int findMin(vector<int>& arr) {
        // complete the function here
        int n = arr.size();
        int min = INT_MAX;
        for(int i =0; i<n; i++){
            if(arr[i]<min){
                min = arr[i];
            }
        }
        return min;
    }
};
